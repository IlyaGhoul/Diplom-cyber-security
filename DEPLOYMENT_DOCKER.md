# 🐳 Docker Compose (Linux / Windows)

Этот вариант запускает **API (FastAPI)** и **SQLite** (файл данных) в контейнере и работает одинаково на:
- **Linux** (Docker Engine + Docker Compose)
- **Windows** (Docker Desktop + WSL2)

## 1) Подготовка

1. Установите Docker и Docker Compose.
2. В корне проекта создайте файл `.env`:
   - скопируйте `.env.example` → `.env`
   - при необходимости задайте `CYBER_VIS_DB_PATH` (по умолчанию `/data/login_attempts.db`)
   - если нужен другой порт на хосте, задайте `API_PORT` (например `42635`)

## 2) Запуск

В корне репозитория:

```bash
docker compose up -d --build
```

Проверка:

```bash
docker compose ps
docker compose logs -f api
```

API документация:
- `http://localhost:8000/docs` (или `http://localhost:<API_PORT>/docs`)

## 3) Подключение клиентов

### Desktop app
Укажите адрес API через переменную окружения:

- Linux/macOS:
  ```bash
  export CYBER_VIS_API_BASE="http://localhost:8000"
  ```
- Windows (PowerShell):
  ```powershell
  $env:CYBER_VIS_API_BASE = "http://localhost:8000"
  ```

Если сервер на удалённой машине, подставьте её IP/домен:
`http://<server-ip>:<API_PORT>` (или `https://<domain>` если настроите TLS).

### Web monitor (HTML)
Обновите URL в HTML (или используйте скрипт):

```bash
python update_api_urls.py http://localhost:8000 "D:\Диплом__НА__2026__год\Программирование\Host\cybattack.ru.github.io"
```

Если ваш сайт лежит в отдельной папке (например GitHub Pages), передайте путь вторым аргументом:

```bash
python update_api_urls.py https://api.example.com "D:\Диплом__НА__2026__год\Программирование\Host\cybattack.ru.github.io"
```

## 4) Данные SQLite

Файл базы хранится в volume `sqlite_data` и переживает перезапуски контейнера.

## 5) Удалённый Linux сервер (заметки)

- Откройте доступ к порту `8000/tcp` на фаерволе (если без reverse-proxy).
- Для нормального **HTTPS/WSS** лучше поставить reverse-proxy (Caddy/Nginx) и использовать порт `443`.

## 6) HTTPS/WSS через Caddy (нужно для GitHub Pages)

Если фронтенд открывается по **HTTPS** (GitHub Pages всегда HTTPS), то браузер заблокирует запросы на `http://...` и `ws://...`.
Решение — поднять API на `https://...` и `wss://...` через Caddy (Let’s Encrypt):

1. Привяжите домен к IP сервера (A/AAAA запись).
2. Откройте порты `80` и `443` на сервере.
3. В `.env` задайте `CYBER_VIS_DOMAIN` (например `api.example.com`).
4. Запустите:

```bash
docker compose -f compose.yaml -f compose.caddy.yaml up -d --build
```
