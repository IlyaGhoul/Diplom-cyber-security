# Пример переменных окружения для локальной разработки / Docker Compose

# --- Docker Compose: PostgreSQL ---
POSTGRES_DB=login_monitor
POSTGRES_USER=app
POSTGRES_PASSWORD=change_me

# --- Backend: PostgreSQL URL (если запускаете без Docker Compose) ---
# Формат: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://localhost/login_monitor

# --- Опционально: ожидание БД при старте (полезно в Docker Compose) ---
DB_CONNECT_TIMEOUT=3
DB_INIT_MAX_ATTEMPTS=30
DB_INIT_DELAY_SECONDS=1

# --- Desktop client: адрес API ---
CYBER_VIS_API_BASE=http://localhost:8000

# --- (Опционально) HTTPS через Caddy ---
# Нужно, если фронтенд на HTTPS (например GitHub Pages), иначе браузер заблокирует http/ws как mixed content.
# 1) Привяжите домен к IP сервера (A/AAAA запись)
# 2) Откройте порты 80 и 443 на сервере
# 3) Запускайте compose с Caddy: docker compose -f compose.yaml -f compose.caddy.yaml up -d --build
CYBER_VIS_DOMAIN=api.example.com
