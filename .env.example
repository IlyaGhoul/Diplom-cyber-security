# Пример переменных окружения для локальной разработки

# PostgreSQL подключение (обязательно для production)
# Формат: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://localhost/login_monitor

# Опционально: настройки сервера
HOST=0.0.0.0
PORT=8000
RELOAD=true
