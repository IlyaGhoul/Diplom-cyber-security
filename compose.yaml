services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      CYBER_VIS_DB_PATH: /data/login_attempts.db
    volumes:
      - sqlite_data:/data
    ports:
      - "${API_PORT:-8000}:8000"
    restart: unless-stopped

volumes:
  sqlite_data:
